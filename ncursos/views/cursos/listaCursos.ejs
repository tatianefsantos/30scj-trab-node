<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Cadastro de Cursos - Lista</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" />
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body class="text-center">
    <div class="starter-template">
        <p>Bem-vindo,
            <%- usuario.nome %>
        </p>
        <h1>Lista de Cursos</h1>
        <!-- <h4>Selecione uma opção na lista para executá-la</h4> -->
    </div>
    <section>
        <div class="container">
            <table class="table table-striped table-bordered dataTable no-footer">
                <thead>
                    <tr class="bg-primary">
                        <th>Código</th>
                        <th>Descrição</th>
                        <th>CH</th>
                        <th>Categoria</th>
                    </tr>
                </thead>
                <tbody>
                    <% cursos.forEach(function(curso, index) { %>
                        <tr>
                            <td>
                                <%= curso.codigo %>
                            </td>
                            <td>
                                <%= curso.descricao %>
                            </td>
                            <td>
                                <%= curso.ch %>
                            </td>
                            <td>
                                <%= curso.categoria %>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
            <br/>
            <a class="btn btn-small btn-primary btn-block" href='/menu'>Voltar</a>
        </div>
    </section>
    <footer class="footer">
        <small>Sistema de Cadastro de Cursos</small>
    </footer>
</body>

</html>